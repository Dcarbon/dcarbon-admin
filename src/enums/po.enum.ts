enum EUserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  BANNED = 'banned',
  DELETED = 'deleted',
}

enum EContractRole {
  MASTER = 'Master',
  ADMIN = 'Admin',
}

export { EUserStatus, EContractRole };
